<const/items=Array.from({ length: 80 }, (_, index) => `home item ${index + 1}`)/>
<main data-route="home">
  <h1>Home</h1>
  <p>Static route rendered by Marko Run.</p>
  <ul>
    <for|item| of=items>
      <li>${item}</li>
    </for>
  </ul>
</main>
