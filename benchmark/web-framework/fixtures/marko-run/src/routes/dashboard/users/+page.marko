<const/items=Array.from({ length: 120 }, (_, index) => `user item ${index + 1}`)/>
<main data-route="users">
  <h1>Dashboard</h1>
  <h2>Users</h2>
  <ul>
    <for|item| of=items>
      <li>${item}</li>
    </for>
  </ul>
</main>
