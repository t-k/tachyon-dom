<const/items=Array.from({ length: 120 }, (_, index) => `order item ${index + 1}`)/>
<main data-route="orders">
  <h1>Dashboard</h1>
  <h2>Orders</h2>
  <ul>
    <for|item| of=items>
      <li>${item}</li>
    </for>
  </ul>
</main>
