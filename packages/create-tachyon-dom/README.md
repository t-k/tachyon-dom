# create-tachyon-dom

Create a Tachyon DOM starter app:

```sh
npm create tachyon-dom@latest my-app
pnpm create tachyon-dom my-app
```

The generated app contains route-local `.td` templates, a Vite config, TypeScript config, and scripts for `dev`, `typecheck`, `build`, and `preview`.
